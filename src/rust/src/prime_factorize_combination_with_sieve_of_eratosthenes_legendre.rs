use crate::{
    legendre_formula_recurse_usize::*,
    sieve_of_eratosthenes_enumerate_primes_usize::*,
};
// pub fn prime_factorize(n: usize, k: usize) -> Vec<(usize, usize)> {
//     let mut factors = vec![];
//     if k > n {
//         return factors;
//     }
//     // for p in enumerate_primes(
// }
#[cfg(test)]
mod tests {
    #[test]
    fn test() {}
}
